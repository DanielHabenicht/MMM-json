/* eslint no-multi-spaces: 0 */
const expect = require("chai").expect;
const Module = require("./node_helper.js");
let node_helper = new Module();
describe("Functions module MMM-json", function () {
  before(function () {});

  describe("roundValue", function () {
    describe("this.config.roundTemp is true", function () {
      before(function () {});
      node_helper.start();
    });
  });
});
